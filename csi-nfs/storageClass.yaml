apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi-synology
provisioner: nfs.csi.k8s.io
parameters:
  server: 10.0.0.3
  share: /volume1/pv-test
reclaimPolicy: Delete
volumeBindingMode: Immediate
mountOptions:
  - nfsvers=4.1
  - hard